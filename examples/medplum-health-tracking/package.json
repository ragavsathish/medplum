{
  "name": "medplum-health-tracking",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest:acceptance": "rimraf dhf/allure-results",
    "test": "vitest run",
    "test:acceptance": "MEDPLUM_ACCEPTANCE=1 vitest run --config tests/acceptance/vitest.config.mts --maxWorkers=2 --maxConcurrency=2 tests/acceptance/accounts/*.acceptance.test.ts",
    "test:acceptance:stack": "docker compose -f docker-compose.acceptance.yml up -d --wait --pull never && npm run test:acceptance",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@medplum/core": "5.1.37",
    "@medplum/fhirtypes": "5.1.37",
    "@types/express": "5.0.6",
    "typescript": "6.0.3",
    "vitest": "4.1.11",
    "zod": "3.25.76",
    "zod-to-json-schema": "^3.25.2"
  },
  "dependencies": {
    "express": "5.2.1"
  }
}
